package jStrings;

public class Desencriptar
{
    public static void main(String[] args)
    {
        //declarar el mensaje, la clave, y el mensajeEncriptado
        String mensaje = "rfsfsf jx ozjajx? t anjwsjx";
        //fjt dcsph wxgpc?
        //rqpvg c jcegt nc vctgc!
        //rfsfsf jx ozjajx? t anjwsjx?

        int clave15 = 15;
        int clave14 = 14;
        int clave13 = 13;
        int clave12 = 12;
        int clave11 = 11;
        int clave10 = 10;
        int clave9 = 9;
        int clave8 = 8;
        int clave7 = 7;
        int clave6 = 6;
        int clave5 = 5;
        int clave4 = 4;
        int clave3 = 3;
        int clave2 = 2;
        int clave1 = 1;
        String mensajeEncriptado15 = "";
        String mensajeEncriptado14 = "";
        String mensajeEncriptado13 = "";
        String mensajeEncriptado12 = "";
        String mensajeEncriptado11 = "";
        String mensajeEncriptado10 = "";
        String mensajeEncriptado9 = "";
        String mensajeEncriptado8 = "";
        String mensajeEncriptado7 = "";
        String mensajeEncriptado6 = "";
        String mensajeEncriptado5 = "";
        String mensajeEncriptado4 = "";
        String mensajeEncriptado3 = "";
        String mensajeEncriptado2 = "";
        String mensajeEncriptado1 = "";




        //Desencriptar 15
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave15);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado15 = mensajeEncriptado15 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 15 es  " + mensajeEncriptado15);


        //Desencriptar 14
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave14);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado14 = mensajeEncriptado14 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 14 es  " + mensajeEncriptado14);


        //Desencriptar 13
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave13);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado13 = mensajeEncriptado13 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 13 es  " + mensajeEncriptado13);


        //Desencriptar 12
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave12);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado12 = mensajeEncriptado12 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 12 es  " + mensajeEncriptado12);


        //Desencriptar 11
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave11);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado11 = mensajeEncriptado11 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 11 es  " + mensajeEncriptado11);



        //Desencriptar 10
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave10);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado10 = mensajeEncriptado10 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 10 es  " + mensajeEncriptado10);


        //Desencriptar 9
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave9);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado9 = mensajeEncriptado9 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 9 es  " + mensajeEncriptado9);



        //Desencriptar 8
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave8);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado8 = mensajeEncriptado8 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 8 es  " + mensajeEncriptado8);


        //Desencriptar 7
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave7);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado7 = mensajeEncriptado7 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 7 es  " + mensajeEncriptado7);



        //Desencriptar 6
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave6);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado6 = mensajeEncriptado6 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 6 es  " + mensajeEncriptado6);



        //Desencriptar 5
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave5);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado5 = mensajeEncriptado5 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 5 es  " + mensajeEncriptado5);


        //Desencriptar 4
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave4);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado4 = mensajeEncriptado4 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 4 es  " + mensajeEncriptado4);


        //Desencriptar 3
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave3);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado3 = mensajeEncriptado3 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 3 es  " + mensajeEncriptado3);



        //Desencriptar 2
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave2);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado2 = mensajeEncriptado2 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 2 es  " + mensajeEncriptado2);



        //Desencriptar 1
        for (int i = 0; i < mensaje.length(); i++)
        {
            char actual;
            if (mensaje.charAt(i) >= 'a' && mensaje.charAt(i) <= 'z')
            {
                actual = (char) (mensaje.charAt(i) - clave1);
                if (actual < 'a')
                {
                    actual = (char) (actual + 26);
                }
            } else {
                actual = mensaje.charAt(i);
            }
            mensajeEncriptado1 = mensajeEncriptado1 + actual;
        }
        //imprimirlo
        System.out.println("El mensaje encriptado con clave 1 es  " + mensajeEncriptado1);



    }
}

